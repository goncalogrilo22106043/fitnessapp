{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm run vercel:build:api",
  "builds": [
    {
      "src": "apps/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/api/index.ts"
    }
  ]
}
